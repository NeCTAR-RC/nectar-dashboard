"quota_admin": "role:provisioner"
"read_only": "role:read_only"

"volume_extension:quotas:show": "rule:admin_or_owner or rule:read_only"
"volume_extension:quotas:update": "rule:quota_admin"
"volume_extension:quotas:delete": "rule:quota_admin"
"volume_extension:quota_classes": "rule:quota_admin or rule:read_only"

"volume:get": "rule:admin_or_owner or rule:read_only"
"volume:get_all": "rule:admin_or_owner or rule:read_only"
"volume:get_volume_metadata": "rule:admin_or_owner or rule:read_only"
"volume:get_volume_admin_metadata": "rule:admin_api or rule:read_only"
"volume:get_snapshot": "rule:admin_or_owner or rule:read_only"
"volume:get_all_snapshots": "rule:admin_or_owner or rule:read_only"
"volume:get_snapshot_metadata": "rule:admin_or_owner or rule:read_only"
"volume_extension:volume_encryption_metadata": "rule:admin_or_owner or rule:read_only"
"volume_extension:extended_snapshot_attributes": "rule:admin_or_owner or rule:read_only"
"volume_extension:volume_image_metadata": "rule:admin_or_owner or rule:read_only"
"volume:get_transfer": "rule:admin_or_owner or rule:read_only"
"volume:get_all_transfers": "rule:admin_or_owner or rule:read_only"
"backup:get": "rule:admin_or_owner or rule:read_only"
"backup:get_all": "rule:admin_or_owner or rule:read_only"
"group:get": "rule:admin_or_owner or rule:read_only"
"group:get_all": "rule:admin_or_owner or rule:read_only"
"group:get_group_snapshot": "rule:admin_or_owner or rule:read_only"
"group:get_all_group_snapshots": "rule:admin_or_owner or rule:read_only"
"message:get": "rule:admin_or_owner or rule:read_only"
"message:get_all": "rule:admin_or_owner or rule:read_only"
