"read_only": "role:read_only"
"get_floatingip": "rule:admin_or_owner or rule:read_only or rule:read_only"
"get_floatingip_port_forwarding": "rule:admin_or_ext_parent_owner or rule:read_only"
"get_port": "rule:context_is_advsvc or rule:admin_owner_or_network_owner or rule:read_only"
"get_dhcp-networks": "rule:admin_only or rule:read_only"
"get_log": "rule:admin_only or rule:read_only"
"get_port:binding:host_id": "rule:admin_only or rule:read_only"
"get_security_group_rule": "rule:admin_or_owner or rule:read_only"
"get_subnet": "rule:admin_or_owner or rule:shared or rule:read_only"
"get_port:resource_request": "rule:admin_only or rule:read_only"
"get_service_profile": "rule:admin_only or rule:read_only"
"get_loggable_resource": "rule:admin_only or rule:read_only"
"get_metering_label_rule": "rule:admin_only or rule:read_only"
"get_address_scope": "rule:admin_or_owner or rule:shared_address_scopes or rule:read_only"
"get_port:binding:vif_details": "rule:admin_only or rule:read_only"
"get_port:binding:vif_type": "rule:admin_only or rule:read_only"
"get_agent": "rule:admin_only or rule:read_only"
"get_network:provider:segmentation_id": "rule:admin_only or rule:read_only"
"get_router": "rule:admin_or_owner or rule:read_only"
"get_l3-agents": "rule:admin_only or rule:read_only"
"get_router:distributed": "rule:admin_only or rule:read_only"
"get_network": "rule:admin_or_owner or rule:shared or rule:external or rule:context_is_advsvc or rule:read_only"
"get_subnet:segment_id": "rule:admin_only or rule:read_only"
"get_router:ha": "rule:admin_only or rule:read_only"
"get_network:provider:physical_network": "rule:admin_only or rule:read_only"
"get_network_segment_range": "rule:admin_only or rule:read_only"
"get_rbac_policy": "rule:admin_or_owner or rule:read_only"
"get_loadbalancer-hosting-agent": "rule:admin_only or rule:read_only"
"get_auto_allocated_topology": "rule:admin_or_owner or rule:read_only"
"get_network:provider:network_type": "rule:admin_only or rule:read_only"
"get_network_ip_availability": "rule:admin_only or rule:read_only"
"get_metering_label": "rule:admin_only or rule:read_only"
"get_port:binding:profile": "rule:admin_only or rule:read_only"
"get_trunk": "rule:admin_or_owner or rule:read_only"
"get_network:segments": "rule:admin_only or rule:read_only"
"get_subnetpool": "rule:admin_or_owner or rule:shared_subnetpools or rule:read_only"
"get_l3-routers": "rule:admin_only or rule:read_only"
"get_alias_dscp_marking_rule": "rule:get_policy_dscp_marking_rule or rule:read_only"
"get_segment": "rule:admin_only or rule:read_only"
"get_dhcp-agents": "rule:admin_only or rule:read_only"
"get_agent-loadbalancers": "rule:admin_only or rule:read_only"
